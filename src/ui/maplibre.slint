// MapLibre Native Slint components (vendored from maplibre/maplibre-native-slint).
// m-map-view.slint here carries one local addition: a `render-active` property
// to pause the render loop while the screensaver overlay is up.

export { MMapView } from "m-map-view.slint";
export { MMapAdapter } from "m-map-adapter.slint";
